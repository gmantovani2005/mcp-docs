apiVersion: v1
kind: Service
metadata:
  name: mcp-docs
  labels:
    app: mcp-docs
spec:
  type: ClusterIP
  selector:
    app: mcp-docs
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
