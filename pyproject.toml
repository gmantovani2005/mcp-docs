[project]
name = "mcp-docs"
version = "0.1.0"
description = "MCP Server to expose Markdown documentation to LLMs"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=1.0.0",
]

[project.scripts]
mcp-docs = "main:main"
